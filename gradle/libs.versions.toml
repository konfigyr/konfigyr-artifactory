[versions]
junit = "6.1.2"

[libraries]
jspecify = { module = "org.jspecify:jspecify", version = "1.0.0" }
jackson-databind = { module = "tools.jackson.core:jackson-databind", version = "3.2.0" }

assert4j = { module = "org.assertj:assertj-core" , version = "3.27.7" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }
junit-launcher = { module = "org.junit.platform:junit-platform-launcher", version.ref = "junit" }
